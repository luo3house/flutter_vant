{
  "private": true,
  "scripts": {
    "dev:web": "flutter run -d web-server --web-port=8080 --web-hostname=0.0.0.0",
    "dev:android": "flutter run -d `npx adbx d`",
    "dev:android:perf": "flutter run -d `npx adbx d` --profile",
    "build:web": "flutter build web",
    "postinstall": "flutter pub get"
  }
}
